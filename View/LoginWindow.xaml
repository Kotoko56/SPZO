<Window x:Class="SPZO.View.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Logowanie" Height="200" Width="340"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" WindowStyle="SingleBorderWindow">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0 0 0 8">
            <TextBlock Text="Nazwa uzytkownika" Margin="0 0 0 4"/>
            <TextBox x:Name="UsernameTextBox" />
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0 0 0 12">
            <TextBlock Text="Haslo" Margin="0 0 0 4"/>
            <PasswordBox x:Name="PasswordBox" />
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Zaloguj" Width="90" Margin="0 0 8 0" Click="OkButton_Click"/>
            <Button Content="Anuluj" Width="90" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>